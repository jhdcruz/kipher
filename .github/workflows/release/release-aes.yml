name: release / aes

on:
  workflow_call:
  push:
    tags:
      - "aes-**"

concurrency:
  group: release-aes
  cancel-in-progress: true

env:
  GRADLE_OPTS: -Dorg.gradle.jvmargs="-XX:MaxMetaspaceSize=2g"
  JDK_VERSION: 19
  GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
  GPG_PASSWORD: ${{ secrets.GPG_PASSWORD }}
  GPG_PRIVATE_KEY: ${{ secrets.GPG_PRIVATE_KEY }}

jobs:
  publish:
    runs-on: ubuntu-latest
    permissions:
      contents: write
      packages: write

    steps:
      - uses: actions/checkout@v3

      - name: Set up JDK ${{ env.JDK_VERSION }}
        uses: actions/setup-java@v3
        with:
          java-version: ${{ env.JDK_VERSION }}
          distribution: temurin

      - name: Validate Gradle wrapper
        uses: gradle/wrapper-validation-action@v1

      - uses: gradle/gradle-build-action@v2

      - name: remove SNAPSHOT on gradle.properties version
        run: |
          cd kipher-aes
          sed -i '/^VERSION=/ s/-SNAPSHOT//' gradle.properties

      - name: Get version from gradle.properties
        id: version_name
        shell: bash
        run: |
          cd kipher-aes
          echo "version=$(grep '^VERSION=' gradle.properties | cut -d'=' -f2)" >> $GITHUB_OUTPUT

      - name: Gradle build
        run: ./gradlew kipher-aes:clean kipher-aes:build --scan

      - name: Publish kipher-aes on GPR
        run: ./gradlew kipher-aes:publishAllPublicationsToGitHubPackagesRepository

      - name: Publish kipher-aes on Sonatype
        run: ./gradlew kipher-aes:publishToSonatype kipher-aes:closeSonatypeStagingRepository
        env:
          SONATYPE_USER: ${{ secrets.SONATYPE_USERNAME }}
          SONATYPE_PASS: ${{ secrets.SONATYPE_PASSWORD }}

      - name: Get Changelog Entry
        id: changelog_reader
        uses: mindsers/changelog-reader-action@v2
        with:
          validation_level: warn
          version: ${{ steps.version_name.outputs.version }}
          path: docs/CHANGELOG-aes.md

      - name: Create release
        uses: ncipollo/release-action@v1
        if: startsWith(github.ref, 'refs/tags/')
        with:
          tag: aes-${{ steps.changelog_reader.outputs.version }}
          name: kipher-aes ${{ steps.changelog_reader.outputs.version }}
          body: ${{ steps.changelog_reader.outputs.changes }}
          prerelease: ${{ steps.changelog_reader.outputs.status == 'prerelease' }}
          draft: ${{ steps.changelog_reader.outputs.status == 'unreleased' }}
          allowUpdates: true
          artifactErrorsFailBuild: false
          artifacts: |
            kipher-aes/build/libs/*

  docs:
    uses: ./.github/workflows/docs.yml
    secrets: inherit
